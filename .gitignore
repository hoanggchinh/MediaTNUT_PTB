# --- 1. Cấu hình PyCharm / JetBrains (BẮT BUỘC CHẶN) ---
# Thư mục này chứa cài đặt giao diện của riêng máy bạn
.idea/
*.iml
*.iws
*.ipr
out/

# Nếu bạn có dùng kèm Python trong dự án
__pycache__/
*.py[cod]
venv/
.venv/
env/

# --- 2. Node.js & Electron (RẤT NẶNG - PHẢI CHẶN) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- 3. Bảo mật (TUYỆT ĐỐI KHÔNG UP) ---
.env
.env.local
.env.*

# --- 4. File tạm & Rác hệ thống ---
temp/
renderer/temp/
.DS_Store
Thumbs.db
ehthumbs.db

# --- 5. File Build (File .exe/.dmg khi xuất bản) ---
dist/
build/
release/